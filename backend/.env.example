# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
# Network Configuration for Mobile Access
# The server will automatically detect your local IP
# Make sure your mobile device is on the same WiFi network
# Blockchain Configuration
FABRIC_NETWORK_PATH=./fabric-network
CHAINCODE_NAME=farmtrace
CHANNEL_NAME=farmtracechannel
# JWT ConfigurationJWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRE=24h
# Database Configuration (if using external DB)
DATABASE_URL=mongodb://localhost:27017/farmtrace
# QR Code Configuration
QR_CODE_SIZE=300
QR_CODE_FORMAT=png
# Frontend URL (will be auto-detected)
FRONTEND_URL=auto
# Logging
LOG_LEVEL=info
# CORS - Allow all origins for development (restrict in production)
ALLOWED_ORIGINS=*
# Mobile Testing
# After starting the server, test mobile connectivity:
# 1. Note the IP address shown in console (e.g., 192.168.1.100)
# 2. On mobile browser, visit: http://YOUR_IP:3001/health
# 3. If successful, QR codes will work for mobile scanning
# 4. Ensure Windows Firewall allows port 3001
